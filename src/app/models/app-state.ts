/*
import { CharacterMap } from '../sprites/character-map';

export enum AppState {
    GAME_INTRO = 0,
    GAME_STARTING,
    GAME_IN_PROGRESS
};

export class AppStateModel {
    static state:AppState = AppState.GAME_STARTING;//AppState.GAME_INTRO;
    static character:CharacterMap;
    static levelNumber:number;

    static numLives: number = 3;
    static numTP: number = 0;
    static numPlungers: number = 0;
};

*/